import { GetTrackCategoryPipe } from './get-track-category-pipe';

describe('GetTrackCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTrackCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});

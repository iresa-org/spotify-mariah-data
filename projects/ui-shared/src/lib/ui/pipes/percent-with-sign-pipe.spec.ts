import { PercentWithSignPipe } from './percent-with-sign-pipe';

describe('PercentWithSignPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentWithSignPipe();
    expect(pipe).toBeTruthy();
  });
});
